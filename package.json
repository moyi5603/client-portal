{
  "name": "client-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r dist/* ../dist/",
    "vercel-build": "cd client && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/"
  }
}